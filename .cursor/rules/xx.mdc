---
alwaysApply: true
---
项目进行代码请注意更新升级，UI和布局不需要改动，改动的话必须要颜色一样，项目所有依赖环境，这个项目是2025年11月创建的，注意一下就行

请把代码写全补全，请为所有功能代码写上注释，注释以英文为标准

单个文件不能超过500行代码，单个文件业务逻辑往里面添加需要创建新的文件进行分包

实行代码管理，避免出现混乱

需要创建文件目录时请看项目里面是否有相同的文件目录，存放文件目录归属，每一个功能文件都有项目的管理目录，前端和后端都有一个src文件管理，存放时请注意看文件位置

请不要写模拟数据硬编码，根据查询数据库或者真实API获取数据

单个文件代码量不能超过500

完成任务更新修改过的项目，也就是维护文档，禁止写多余的文档

了解项目之后禁止写文档

写完代码之后必须进行代码审查，检查是否有错误逻辑

禁止写多BUG，严格进行代码逻辑审查